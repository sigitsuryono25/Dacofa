<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.surelabsid.lti.dacofa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dacofa">
        <activity
            android:name=".ui.isidata.DetailHasilTangkapanActivity"
            android:exported="true" />
        <activity
            android:name=".ui.isidata.IsiDataActivity"
            android:exported="true" />
        <activity
            android:name=".ui.lihatdata.LihatDataActivity"
            android:exported="true" />
        <activity
            android:name=".ui.landing.LandingActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>